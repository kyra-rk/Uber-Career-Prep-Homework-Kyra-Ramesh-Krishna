/* Node Class for singly linked list */
    public class LNode {
        int data;
        LNode next;
  
        // Constructor to create a new node
        LNode(int d)
        {
            data = d;
            next = null;
        }
    }